@model IEnumerable<ShopOnline.Models.Customer>

@{
    ViewBag.Title = "Quản lý Khách hàng";
    Layout = "~/Views/Shared/_LayoutAdmin.cshtml";
}

<div class="card">
    <div class="card-header">
        <h3>Danh sách Khách hàng</h3>
    </div>
    <div class="card-body">
        <div style="margin-bottom: 20px;">
            <a href="@Url.Action("Create")" class="btn btn-success">
                <span class="glyphicon glyphicon-plus"></span> Thêm mới
            </a>
        </div>

        @if (Model != null && Model.Any())
        {
            <table class="table table-bordered table-hover">
                <thead>
                    <tr>
                        <th style="width: 10%;">ID</th>
                        <th style="width: 25%;">Tên khách hàng</th>
                        <th style="width: 20%;">Số điện thoại</th>
                        <th style="width: 25%;">Email</th>
                        <th style="width: 20%; text-align: center;">Thao tác</th>
                    </tr>
                </thead>
                <tbody>
                    @foreach (var item in Model)
                    {
                        <tr>
                            <td>@item.IDCus</td>
                            <td><strong>@item.NameCus</strong></td>
                            <td>
                                <span class="glyphicon glyphicon-phone"></span> @item.PhoneCus
                            </td>
                            <td>
                                <span class="glyphicon glyphicon-envelope"></span> @item.EmailCus
                            </td>
                            <td style="text-align: center;">
                                <a href="@Url.Action("Details", new { id = item.IDCus })" class="btn btn-info btn-sm">
                                    <span class="glyphicon glyphicon-info-sign"></span> Chi tiết
                                </a>
                                <a href="@Url.Action("Edit", new { id = item.IDCus })" class="btn btn-warning btn-sm">
                                    <span class="glyphicon glyphicon-edit"></span> Sửa
                                </a>
                                <a href="@Url.Action("Delete", new { id = item.IDCus })" class="btn btn-danger btn-sm">
                                    <span class="glyphicon glyphicon-trash"></span> Xóa
                                </a>
                            </td>
                        </tr>
                    }
                </tbody>
            </table>
        }
        else
        {
            <div class="alert alert-info">
                <strong>Thông báo:</strong> Chưa có khách hàng nào.
                <a href="@Url.Action("Create")">Thêm khách hàng đầu tiên</a>
            </div>
        }
    </div>
</div>
